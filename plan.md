# 마이페이지 개발 계획

> 상태: 구현 완료·시각 승인 대기
> 작성일: 2026-08-22
> 대상: MarshMallow 프런트엔드 마이페이지 및 공통 AppShell 진입 동작

## 1. 목표와 완료 조건

Figma의 최종 마이페이지 화면과 회원 정보 수정 화면을 현재 React/MUI 구조에 맞게 구현한다. 화면에 필요한 데이터는 `localhost:3000/swagger`에서 확인된 API와 연결하고, Swagger에 없는 기능은 추정 API로 구현하지 않는다.

완료 조건은 다음과 같다.

- `/my-page` 직접 접근, AppShell 아바타 메뉴 진입, 브라우저 뒤로/앞으로가 동작한다.
- 마이페이지가 공통 상단 영역과 외부 패널을 복제하지 않고 `AppShell`을 사용한다.
- 프로필, 매수·매도 일지 현황, 가상계좌, 관심종목, 이용 현황, 최근 주문 영역이 Figma의 정보 구조와 반응형 레이아웃을 따른다.
- 회원 정보 수정 화면에 저장, 취소, 유효성 검사, 비밀 필드 보호가 적용된다.
- Swagger로 확정된 데이터는 실제 API를 사용하고, 계약이 없는 데이터는 명시적인 미지원 상태 또는 승인된 목업으로만 표시한다.
- 로딩, 빈 결과, 오류, 인증 만료, 늦은 응답을 처리한다.
- 타입 검사, lint, build, 변경 파일 포맷 검사와 주요 브라우저 시나리오를 통과한다.

## 2. 확인한 기준

### Figma

- 파일: `I6LiJUhTSEXkGDYdGPQlKF`, Page 1 `0:1`
- 최종 마이페이지: `987:7124`
- 월 미선택 상태: `98:1107`
- 프로필 카드: `987:7341`
- 매수·매도 일지 현황: `987:7284`
- 가상계좌: `987:7243` (Figma 레이어명은 `내 계좌`)
- 관심종목: `987:7203`
- 이용 현황과 주문 내역: `987:7145`
- 프로필 팝업: `129:558`
- 회원 정보 수정: `824:6502`

1920×1080 기준 화면은 다음 구조다.

1. 왼쪽: 프로필 카드와 연간 월별 매수·매도 일지 현황
2. 가운데 위: 가상계좌
3. 가운데 아래: 관심종목
4. 오른쪽: 이용 현황과 최근 주문 내역
5. 월 선택 시 해당 월의 일지를 GitHub 브랜치 그래프 형태로 표시하고, 전체/매도/매수 필터와 최신순/금액순 정렬 제공
6. 프로필 수정 진입 시 이름·이메일, Google 연동, 비밀번호, API Key/Secret, 프로필 이미지, 저장/취소 표시
7. 상단 아바타 클릭 시 사용자 정보, 마이페이지 이동, 로그아웃 메뉴 표시

### Swagger

2026-08-22에 `http://localhost:3000/swagger-json`을 직접 확인했다.

| 화면 기능                     | 확인된 API                                                                      | 판단                                                           |
| ----------------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| 관심종목 목록/해제            | `GET /users/me/favorite-stocks`, `DELETE /users/me/favorite-stocks/{stockCode}` | 목록의 종목 코드·이름은 연동 가능                              |
| 관심종목 현재가/산업/로고     | 없음. `market`도 현재 항상 `null`                                               | 별도 종목/시세 계약 필요                                       |
| 포트폴리오 목록               | `GET /assets/portfolios`                                                        | 이름과 정렬 순서만 연동 가능                                   |
| 가상계좌 잔액·보유자산        | 없음                                                                            | 가상계좌 요약 계약 필요                                        |
| 매수·매도 일지 목록/월별 현황 | `GET /diaries`                                                                  | 기간과 페이지 필터로 일지 현황을 연동 가능                     |
| 최근 주문                     | `GET /orders`                                                                   | 응답이 `object[]`로만 선언되어 안전한 화면 타입을 만들 수 없음 |
| 사용자 프로필 조회/수정       | 없음                                                                            | 프로필 계약 필요                                               |
| 비밀번호/프로필 이미지 수정   | 없음                                                                            | 수정 계약 필요                                                 |
| Google 연결/해제              | 없음                                                                            | OAuth 계정 연결 계약 필요                                      |
| API Key/Secret 저장           | 없음                                                                            | 비밀정보 전용 계약 필요                                        |
| 접속수/이용 통계              | 없음                                                                            | 통계 계약 필요                                                 |
| 로그아웃                      | 없음                                                                            | refresh cookie까지 폐기하는 로그아웃 계약 필요                 |

추가로 확인된 계약상 주의점은 다음과 같다.

- `DiaryPreviewDto`는 숫자 `diaryId`, `BUY | SELL`인 `type`, `date`, `avgPrice`, `quantity`, 종목 정보를 제공한다. 페이지에서는 이 DTO를 직접 렌더링하지 않고 일지 현황용 화면 모델로 변환한다.
- 일지 목록은 `page`가 0부터 시작하고 `size` 최대값이 20이며 `hasNext`를 제공한다. 연간 월별 현황은 필요한 페이지를 모두 순회해야 한다.
- `GET /diaries`에는 매수/매도 유형 필터가 없으므로 전체 일지를 받은 뒤 `type`으로 화면에서 필터링한다. 금액순은 `avgPrice`가 존재할 때 `avgPrice × quantity`로 계산하고, 가격이 없는 일지는 별도 처리한다.
- 관심종목 DTO는 `stockCode`, `stockName`, `market`, `createdAt`만 제공한다.
- 포트폴리오 DTO는 `id`, `name`, `sortOrder`, `createdAt`만 제공한다.
- Bearer 보안 스킴은 정의되어 있지만 각 operation의 보안 요구는 Swagger에 표시되지 않는다.
- 성공 응답의 `{ data: T }` 래핑과 오류의 `code/message/traceId`는 저장소 문서에는 있으나 현재 `apiFetch`와 Swagger 스키마에 완전히 반영되어 있지 않다.
- 현재 프런트의 Google 콜백은 `/auth/google`을 호출하지만 Swagger에는 `/auths/signup`, `/auths/login`만 있다. 인증 계약도 함께 정리해야 한다.

### 현재 프런트 구조

- 공통 셸: [`src/components/AppShell/AppShell.tsx`](./src/components/AppShell/AppShell.tsx)
- 임시 라우팅: [`src/app.tsx`](./src/app.tsx), [`src/lib/navigation.ts`](./src/lib/navigation.ts)
- API 공통 계층: [`src/lib/api.ts`](./src/lib/api.ts)
- 디자인 토큰: [`src/theme/tokens.ts`](./src/theme/tokens.ts)
- 공용 카드/입력/버튼: `BaseCard`, `TextField`, `Button`
- 내 계좌 화면은 이미 `/account`에 있으며 현재 목업 기반이다. 마이페이지의 가상계좌 요약은 이를 복제하지 않고 화면 모델 또는 추후 API 계층을 공유한다.
- Zustand는 이 화면에 필요하지 않다. 서버 데이터와 페이지 내부 선택 상태는 페이지와 기능 API 계층에서 관리한다.

## 3. 승인할 제품·기술 결정

이 계획의 승인은 아래 결정을 함께 승인하는 것으로 본다.

1. 마이페이지 기본 경로는 `/my-page`, 수정 화면은 `/my-page/edit`로 사용한다.
2. 정식 라우터를 새로 도입하지 않고 현재 `app.tsx`와 `navigate()`에 최소 분기를 추가한다.
3. 마이페이지는 `AppShell currentPageLabel="마이페이지"`를 사용한다. 기존 공통 탭 이름과 다른 페이지 동작은 이번 범위에서 전면 개편하지 않는다.
4. AppShell 아바타를 접근 가능한 버튼으로 바꾸고 공통 프로필 팝업을 추가한다. 이 변경은 AppShell을 사용하는 홈, 뉴스, 내 계좌, 매매일지 화면도 회귀 확인한다.
5. Swagger로 확정된 관심종목, 포트폴리오, 매매 일지 데이터는 실 API로 연결한다.
6. 현재가·산업, 가상계좌 잔액·보유자산, 프로필 수정, 통계, Google 연결, API Key/Secret, 타입이 없는 주문 응답은 백엔드 계약이 생길 때까지 영구 타입이나 요청 코드를 추측하지 않는다.
7. 기본값은 실 API 우선으로 두고, 일지·관심종목·포트폴리오 요청이 실패한 영역만 `src/pages/my-page/mock-data.ts`의 목업으로 자동 대체한다. 대체 사실과 범위는 화면 경고 및 제품 문서에서 명시한다.
8. 로그아웃은 백엔드 계약 전에는 access token과 사용자 캐시를 지우는 임시 동작까지만 가능하다. refresh cookie를 포함한 완전한 로그아웃은 API 추가 후 완료 처리한다.
9. API Key/Secret은 localStorage, URL, 콘솔, 오류 메시지에 저장하거나 출력하지 않는다. 서버가 기존 값을 그대로 반환하지 않고 마스킹/등록 여부만 반환하는 계약을 요구한다.

## 4. 범위

### 포함

- 마이페이지 기본/월 선택/로딩/빈 결과/오류 화면
- 회원 정보 수정 화면과 폼 상태
- AppShell 아바타 메뉴, 마이페이지 진입, 임시 로그아웃
- `/my-page`, `/my-page/edit` 경량 라우팅
- 관심종목, 포트폴리오, 매매 일지 API 타입과 변환 로직
- API 공통 래퍼의 인증 헤더, 공통 응답, 구조화 오류 처리 보완
- 공용 토큰과 컴포넌트 재사용, 마이페이지 전용 컴포넌트 분리
- 관련 문서와 Swagger 간극 기록

### 비범위

- 새 라우팅 라이브러리나 서버 상태 라이브러리 도입
- 백엔드 API/DB 구현
- `/account` 화면의 전면 재설계
- 종목 시세를 제3자 API에서 프런트가 직접 조회하는 우회 구현
- API Key/Secret의 브라우저 영구 저장
- Figma의 공통 헤더와 외부 배경을 페이지 내부에서 다시 구현하는 작업

## 5. 구현 순서

### 1단계: 계약과 API 기반 정리

- `apiFetch`가 Bearer token과 `credentials: 'include'`를 일관되게 적용하도록 보완한다.
- 성공 응답은 `{ data: T }`를 안전하게 해제하고, `204`, 비 JSON 응답을 처리한다.
- `ApiError`에 `status`, `code`, `message`, `traceId`를 보존하고 네트워크 실패와 요청 취소를 구분한다.
- 기존 뉴스 호출과 Google 콜백에 대한 회귀 영향을 확인한다.
- 다음 기능 API를 도메인 경계에 맞게 만든다.
  - `features/users`: 관심종목과 향후 사용자 프로필
  - `features/diaries`: 매매 일지 목록 DTO, 쿼리, 화면 모델 변환
  - `features/portfolios`: 포트폴리오 목록 DTO
- Swagger에 없는 필드는 API DTO에 넣지 않는다.

검증: 타입 검사, 성공/401/4xx/5xx/비 JSON/네트워크 실패/취소 시나리오, 기존 뉴스와 로그인 콜백 영향 확인.

### 2단계: AppShell 진입과 라우팅

- `src/app.tsx`에 `/my-page`와 `/my-page/edit` 분기를 구체적인 경로 순서로 추가한다.
- AppShell 아바타를 `aria-label`이 있는 버튼으로 바꾸고 프로필 팝업을 키보드로 열고 닫을 수 있게 한다.
- 팝업에는 저장된 세션 사용자 정보, 마이페이지 이동, 로그아웃을 제공한다.
- 바깥 클릭, Escape, 포커스 복귀를 지원한다.
- 사용자 정보가 없거나 손상됐을 때 안전한 기본 상태를 표시하고 인증 화면으로 유도한다.

검증: 직접 URL, 새로고침, 뒤로/앞으로, 메뉴 키보드 조작, AppShell을 쓰는 모든 인접 페이지.

### 3단계: 마이페이지 레이아웃과 조회 상태

- `src/pages/my-page/MyPage.tsx`는 `AppShell`의 자식으로 패널 내부만 구성한다.
- 페이지 전용 구성 요소는 다음 책임으로 시작하되 실제 복잡도에 따라 합친다.
  - `ProfileCard`
  - `DiaryActivityCard`
  - `VirtualAccountCard`
  - `FavoriteStocksCard`
  - `UsageSummaryCard`
  - `RecentOrdersCard`
- `BaseCard`, 공용 토큰, MUI `sx`를 우선 사용한다.
- 1920×1080에서는 Figma의 3열 구성을 맞추고, 일반 노트북에서는 2열, 작은 화면에서는 1열로 재배치한다.
- 각 카드가 독립적으로 로딩/빈 결과/오류를 표시해 한 API 실패가 페이지 전체를 막지 않게 한다.

검증: Figma 1920×1080 비교, 1440px/1024px/모바일 폭, 긴 이름·긴 금액·빈 목록·부분 실패·스크롤.

### 4단계: 매수·매도 일지 캘린더와 목록

- 현재 연도를 기본으로 12개월 요약을 표시하고 연도 변경을 지원한다.
- `GET /diaries?startDate&endDate&page&size`를 `hasNext`가 끝날 때까지 순회해 월별 매수/매도 일지 존재와 건수를 계산한다.
- 12개월 현황은 Figma의 3×4 히트맵 비율을 유지하고, 선택한 월의 상세 내역은 매수·매도 레인이 분기되는 GitHub 브랜치 그래프형 타임라인으로 표시한다.
- 월 미선택 상태에는 Figma의 안내 문구를 표시한다.
- 월 선택 시 해당 월의 일지 목록을 표시하고 전체/매도/매수 필터와 최신순/금액순 정렬을 적용한다.
- 요청이 겹치면 이전 요청을 취소하고 늦게 끝난 응답이 최신 상태를 덮지 않게 한다.
- 평균 가격, 수량, 계산 가능한 총액, 일지 날짜는 한국어 로케일로 포맷한다.

검증: 일지 0건, 1건, 20건 초과, 월 경계, 윤년, 매수/매도 필터, 가격이 없는 일지, 동일 금액/동일 날짜 정렬.

### 5단계: 관심종목과 포트폴리오

- 관심종목은 실 API 목록을 표시하고 하트 버튼으로 삭제한다.
- 삭제는 낙관적으로 반영하되 실패 시 복구하고 사용자에게 오류를 알린다.
- 행 선택 시 기존 주식 상세 경로 정책이 확정된 경우에만 이동한다.
- 현재가·산업·로고는 계약 전에는 목업 모드에서만 채운다. 실 API 모드에서는 값이 없음을 명확히 표시한다.
- 포트폴리오는 서버 배열 순서를 그대로 사용한다. `sortOrder`를 배열 인덱스로 사용하지 않는다.
- 포트폴리오 이름은 가상계좌 영역에 연결할 수 있지만 잔액이나 보유자산 데이터로 오해하지 않게 별도 화면 모델로 구분한다.

검증: 관심종목 빈 목록/삭제 실패, `market: null`, 포트폴리오 빈 목록/최대 개수, 긴 이름.

### 6단계: 최근 주문, 이용 현황, 프로필 데이터의 계약 연결

- `/orders`에 구체적인 응답 DTO가 Swagger에 추가된 뒤 최근 주문 3건과 전체 주문 이동을 연결한다.
- 접속수와 오늘/누적 거래수는 통계 API가 추가된 뒤 연결한다. 거래수만 `trades.totalCount`에서 임의로 대체하지 않는다.
- 프로필 조회 API가 추가되면 localStorage 사용자 정보는 초기 부트스트랩 용도로만 사용하고 서버 응답을 원본으로 삼는다.
- 가상계좌 요약 API가 추가되면 기존 `/account`와 마이페이지가 같은 도메인 타입과 변환 로직을 공유한다.

검증: 데이터 불일치, 부분 누락, 인증 만료, 통계 0, 주문 0건/3건 초과.

### 7단계: 회원 정보 수정

- `/my-page/edit`에 이름·이메일, Google 연결 상태, 비밀번호 변경, API Key/Secret, 프로필 이미지, 저장/취소를 구성한다.
- `TextField`와 `Button`을 재사용하고 비밀번호 및 secret 필드는 마스킹한다.
- 비밀번호 변경은 신규 비밀번호/확인 일치, 서버 정책, 자동완성을 적용한다.
- 변경 사항이 없으면 저장을 비활성화하고, 저장 중 중복 제출을 막는다.
- 취소와 뒤로가기 시 수정 내용이 있으면 이탈 확인을 제공한다.
- 이미지 업로드 형식/크기와 API credential 저장 방식은 Swagger 계약이 생긴 뒤 연결한다.
- 저장 성공 후 서버 데이터를 다시 조회하고 `/my-page`로 돌아간다.

검증: 유효/무효 이메일, 비밀번호 불일치, 중복 제출, 저장 실패, 취소, 이미지 오류, secret 비노출.

### 8단계: 문서화와 완료 검증

- 실제 확정된 경로와 AppShell 계약을 다음 문서에 반영한다.
  - `docs/product-specs/mypage.md`
  - `docs/architecture/routing.md`
  - `src/components/AppShell/README.md`
  - API 간극이 바뀌면 `docs/contracts/backend-api.md`
- 실행할 자동 검증:
  - `yarn prettier --check <changed-files>`
  - `yarn eslint <changed-ts-files>`
  - `yarn tsc -b`
  - `yarn build`
  - `git diff --check`
- 실행할 수동 검증:
  - Figma 기준 해상도와 일반 노트북/작은 화면
  - 로딩, 빈 결과, 오류, 긴 텍스트
  - 아바타 메뉴, 수정/취소, 월 선택, 필터/정렬, 관심종목 해제
  - 직접 URL, 새로고침, 뒤로/앞으로
  - 홈, 뉴스, 내 계좌, 매매일지 AppShell 회귀
  - 키보드 포커스와 브라우저 콘솔 오류

## 6. 예상 파일 구조

```text
src/
├── components/AppShell/
│   ├── AppShell.tsx
│   └── ProfileMenu.tsx
├── features/
│   ├── portfolios/
│   ├── diaries/
│   └── users/
├── pages/my-page/
│   ├── MyPage.tsx
│   ├── MyPageEditPage.tsx
│   ├── components/
│   ├── types.ts
│   └── mock-data.ts          # 목업 모드를 승인한 경우에만
├── app.tsx
└── lib/
    ├── api.ts
    ├── authSession.ts
    └── navigation.ts
```

미래 사용을 예상한 빈 파일이나 폴더는 만들지 않고, 실제 구현 시 필요한 단위만 생성한다.

## 7. 위험과 대응

| 위험                                                      | 대응                                                                |
| --------------------------------------------------------- | ------------------------------------------------------------------- |
| Swagger 누락으로 Figma 기능을 전부 실데이터화할 수 없음   | 누락 기능을 계약 선행 단계로 두고 추정 API 금지                     |
| AppShell 수정이 모든 로그인 후 화면에 영향                | 프로필 메뉴를 작은 독립 컴포넌트로 두고 모든 사용 화면 회귀 확인    |
| `apiFetch` 변경이 기존 호출을 깨뜨림                      | 공통 응답 해제를 하위 호환 방식으로 구현하고 뉴스/Google 콜백 확인  |
| 연간 일지 조회가 20건을 넘음                              | `hasNext` 기반 페이지 순회, 요청 취소, 필요 시 백엔드 집계 API 요청 |
| Figma의 1920px 절대 배치를 그대로 옮기면 작은 화면이 깨짐 | 시각적 비율을 유지한 CSS grid와 명시적 breakpoint 사용              |
| API Key/Secret 노출                                       | 브라우저 영구 저장 금지, 마스킹, 로그 금지, 서버 재노출 금지 계약   |
| 임시 목업이 영구 데이터처럼 남음                          | 별도 파일, 화면 경고, 데이터 소스 문서, 완료 보고에 잔여 목업 명시  |

## 8. 호환성과 롤백

- `/account`, `/news`, `/journal`, `/home`, 개발용 hash 경로는 유지한다.
- 새 경로는 기존 분기에 추가하며 정식 라우터 도입으로 확대하지 않는다.
- AppShell 프로필 메뉴는 페이지 전용 API를 알지 않으며, 문제가 생기면 아바타 버튼/팝업 변경만 독립적으로 되돌릴 수 있게 한다.
- 마이페이지 각 카드의 데이터 어댑터는 독립시켜 특정 API 연결만 목업/미지원 상태로 전환할 수 있게 한다.
- 서버 계약이 변경되면 DTO와 변환 함수에서 흡수하고 페이지 컴포넌트의 화면 모델은 유지한다.

## 9. 진행 현황

- [x] API 공통 계층과 Swagger 확정 DTO
- [x] `/my-page` 및 `/my-page/edit` 진입과 AppShell 프로필 팝업
- [x] Figma 레이아웃 골격과 관심종목/매매 일지/포트폴리오 API 연결
- [x] 누락 계약을 사용하는 카드의 명시적 목업과 데이터 소스 문서
- [x] 회원 정보 수정 화면과 민감정보 비저장 처리
- [x] TypeScript 프로덕션 빌드
- [x] 포맷, lint, diff 검사 및 프로덕션 빌드
- [x] 로컬 개발 서버 `/my-page` 실행 확인
- [ ] 사용자 최종 시각 승인
